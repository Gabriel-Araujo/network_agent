# Network Agent

# Usage
You must have an openai compatible server running.

The first running the agent will ask for a llm setup

## Build from source
just exec the go build command

```go
go build
```
